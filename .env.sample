# .env.sample
# Copy this file to .env and fill in your values:
#   cp .env.sample .env
# Database Configuration
DB_NAME=animal_db
DB_USER=postgres
DB_PASSWORD=postgres123
DB_HOST=localhost
DB_PORT=5433          # must match host port in docker-compose.yml
